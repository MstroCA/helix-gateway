PORT=
BACKEND_URL=
JWT_SECRET=

# Redis is optional — rate limiting fails open without it
REDIS_ADDR=
REDIS_PASSWORD=

# Allow all origins in development (comma-separated for production)
ALLOWED_ORIGINS=

# Leave empty to disable tracing (set to your OTel collector endpoint)
OTEL_EXPORTER_OTLP_ENDPOINT=
